CREATE TABLE APPDB.pessoa (
                        id INT PRIMARY KEY,
                        nome VARCHAR(255),
                        email VARCHAR(255),
                        data_nascimento DATE,
                        idade INT
);
